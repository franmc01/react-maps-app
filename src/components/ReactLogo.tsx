import reactLogo from '../logo.svg';

export const ReactLogo = () => {
    return (
        <img src={reactLogo}
             alt="logo-react"
             className={"react-logo"}
        />
    );
};

